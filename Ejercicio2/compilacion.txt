gcc main.c servidor.c transaccion.c csv.c utils.c -o servidor -lpthread
gcc cliente.c -o cliente

Opcional
IP=127.0.0.1
PUERTO=5000

./servidor 127.0.0.1 5000
./cliente 127.0.0.1 5000

gcc -Wall -pthread -o servidor main.c servidor.c csv.c transaccion.c utils.c
-Wall activa todos los warnings para ayudarte a detectar problemas.
-pthread es necesario porque usas hilos (pthread).
-o servidor indica que el ejecutable se llamará servidor.
Si todo compila bien, tendrás un ejecutable llamado servidor.

Falta que reciba los clientes concurrentes y en espera por parametro.
Revisar el resto de las consignas por si acaso 